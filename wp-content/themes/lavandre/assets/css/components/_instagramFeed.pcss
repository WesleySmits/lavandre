#instagram-feed {
    padding: 7.5rem 0 0 0;
    line-height: 0;

    h2 {
        text-align: left;
        font-weight: 100;
        line-height: 2.4;
    }
}

.lightwidget-widget {
    background-color: var(--jet);
}
